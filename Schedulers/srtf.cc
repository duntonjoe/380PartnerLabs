#include <stdio.h>
#include <iostream>
#include <string>
#include <vector>

#include "Jobs.cpp"

int main(int argc, char *argv[]) {
  Jobs list;
  int time = 0;
  for(job j : list.getJobs()){

  }
  return EXIT_SUCCESS;
}
